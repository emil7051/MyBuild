# Use /api/v1 for same-origin deployment, or an HTTPS API URL.
# HTTP absolute URLs are only allowed for localhost development.
VITE_API_URL=http://localhost:8000/api/v1
VITE_CLIENT_TELEMETRY_ENABLED=false
VITE_CLIENT_TELEMETRY_URL=
