# Backend Runtime Dependencies
# This file delegates to the root runtime lockfile for deterministic installs.
# Direct dependency declarations live in ../requirements.txt.
# For development tools, use requirements-dev.lock.txt in the project root.

-r ../requirements.lock.txt
