# TCO Web Platform - Runtime Dependencies
# ========================================
# Source-of-truth direct runtime dependencies.
# Install reproducibly with: pip install -r requirements.lock.txt
# For development tooling, use: pip install -r requirements-dev.lock.txt
# Update lockfiles with:
#   uv pip compile requirements.txt --output-file requirements.lock.txt --python-version 3.11 --universal
#   uv pip compile requirements.txt requirements-dev.txt --output-file requirements-dev.lock.txt --python-version 3.11 --universal --no-emit-package pip

# --- Backend Runtime (canonical list; backend/requirements.txt re-exports this) ---

# Web framework
fastapi==0.128.2
uvicorn[standard]==0.34.0

# Data validation
pydantic==2.10.4
pydantic-settings==2.7.1

# Environment
python-dotenv==1.0.0

# Database
sqlalchemy==2.0.46
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
aiosqlite==0.19.0

# Cache
redis==5.0.1

# Observability
opentelemetry-api==1.31.1
opentelemetry-sdk==1.31.1
opentelemetry-exporter-otlp-proto-http==1.31.1

# Security (Phase 4)
slowapi==0.1.9
